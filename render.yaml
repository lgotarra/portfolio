services:
  - type: web
    name: next-js
    runtime: static
    plan: starter
    buildCommand: corepack enable && yarn set version 4.9.1 && yarn install --immutable && yarn build
    startCommand: corepack enable && yarn start
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.14.0
